name: CI/CD - Build and Deploy to Vercel

on:
  push:
    branches:
      - main
      - master
      - dev
      - staging
  pull_request:
    branches:
      - main
      - master
      - dev
      - staging

jobs:
  build:
    name: Build Next.js App
    runs-on: ubuntu-latest

    steps:
      # Checkout repository code
      - name: Checkout code
        uses: actions/checkout@v4

      # Setup pnpm
      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      # Use Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      # Install dependencies
      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Run tests (optional)
      - name: Run tests
        run: pnpm test
        continue-on-error: true # optional - don't fail build if no tests yet

      # Build project
      - name: Build Next.js app
        run: pnpm build

      # (Optional) Export static build for preview
      - name: Export static files
        if: success()
        run: pnpm export || echo "No export script defined"

  # Optional: Deploy using Vercel CLI (if you want manual control from GitHub)
  deploy:
    name: Deploy to Vercel
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Deploy with Vercel CLI
        run: pnpm dlx vercel --prod --yes --token=${{ secrets.VERCEL_TOKEN }}
